# ── Demo mode (works with no backend) ──────────────────────────
# If these are blank, the app runs in demo mode with mock data.
VITE_API_URL=

# ── Supabase (optional — add when ready for real auth + leaderboard) ──
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
